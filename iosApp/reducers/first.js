import {

} from '../actions/action';






  
const initialState = {
  test:'sim',
  
}

export default (state = initialState, action) => {
	switch(action.type) {

		
		
    	default:
      	return state;
	}
}